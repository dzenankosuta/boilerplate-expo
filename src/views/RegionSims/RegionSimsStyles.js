import { StyleSheet } from "react-native";
import { moderateScale } from "react-native-size-matters";

const RegionSimsStyles = StyleSheet.create({
  container: {
    flex: 1,
    width: "100%",
    justifyContent: "flex-start",
    alignItems: "center",
  },
  cardWrapper: {
    width: "100%",
    justifyContent: "center",
    alignItems: "center",
  },
});

export default RegionSimsStyles;
