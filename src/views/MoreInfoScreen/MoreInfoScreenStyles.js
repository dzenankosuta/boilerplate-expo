import { StyleSheet } from "react-native";
import { moderateScale } from "react-native-size-matters";

const MoreInfoScreenStyles = StyleSheet.create({
  container: {
    flex: 1,
  },
});

export default MoreInfoScreenStyles;
